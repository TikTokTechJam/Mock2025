# Training and evaluation

Place offline training, fine-tuning, evaluation scripts, and their safe
configuration here when that work is approved. This directory is not a runtime
service and must not be imported by `apps/api` during normal startup.
