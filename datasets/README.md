# Dataset metadata

This directory contains safe dataset metadata, schemas, split definitions, and
provenance for offline ML work. It does not contain private or raw datasets by
default.

- `manifests/` is reserved for dataset manifest metadata.
- Raw, local, processed, and generated dataset artifacts must remain outside
  Git and in operator-controlled storage.
